'use strict';
define(['scripts/app/collections/todoCollection'], function(todoCollection) {
	describe('todoCollection', function(){
		it('should be true', function(){
			expect(true).toBe(true);
		});
	});
});