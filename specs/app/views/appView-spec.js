'use strict';
xdefine(['underscore', 'jquery', 'backbone' ,'appView'], function(todoCollection) {
	describe('todoCollection', function(){
		it('should be true', function(){
			expect(true).toBe(false);
		});
	});
});