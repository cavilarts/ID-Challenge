'use strict';
xdefine(['underscore', 'jquery', 'backbone' ,'todoView'], function(todoCollection) {
	describe('todoCollection', function(){
		it('should be true', function(){
			expect(true).toBe(false);
		});
	});
});