define([
    'underscore',
    'jquery',
    'backbone',

    ], function(
        _,
        $,
        Backbone
    ){
    // Crime category model
    var CrimeCategoryModel = Backbone.Model.extend({});

    return CrimeCategoryModel;
});


